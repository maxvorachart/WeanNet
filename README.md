# WeanNet

A custom reinforcement learning architecture currently in the simulation phase.

## Overview
WeanNet bridges a custom Luau engine within a simulated environment with external Python testing scripts to evaluate reinforcement learning models.

## Repository Structure
* `/src` - Core Luau neural network modules 
* `/sim` - Simulation environments
* `/tests` - Python evaluation scripts
